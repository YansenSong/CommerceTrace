# 后端代码审查记录

## 2026-07-27

### 1. `run` 流程中"销售额口径"的硬编码澄清

**位置**: `main/run.py` 中 `_requires_clarification` 相关逻辑

**问题**: 单独拦截"销售额口径"这类歧义问题来处理显得很笨。虽然防住了这一个，但其他有歧义的问题并未处理。后续需要从全局层面来考虑解决这类歧义问题，而非逐个打补丁。

**处理状态**: 已移除该硬编码分支及其专用状态、评测指标和用例。通用方案见 `docs/TODO.md`。

### 2. `temporal_coverage_gap_conclusion` 单方面硬编码

**位置**: 结论合成模块中的 `temporal_coverage_gap_conclusion` 函数

**问题**: 同样是单方面考虑、硬编码处理"上个月"这一种场景的时序覆盖问题。与"销售额口径"的问题如出一辙，缺乏通用性。

**处理状态**: 已移除该函数及其专用停止原因和测试。通用方案见 `docs/TODO.md`。
