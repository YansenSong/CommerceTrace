# CommerceTrace

CommerceTrace 是一个本地部署的中文电商经营分析 Data Agent。用户用自然语言查询本地 SQLite 业务数据，系统通过 LLM 生成并执行只读 SQL，把结论、SQL 轨迹和图表返回给用户。上下文规模设计为支持数百张表的业务库。

## Language

**分析任务**：
用户提出的一个需要通过业务数据回答的分析目标；从需求被明确开始，到形成结论、查询工件与可选图表时结束。
_Avoid_: 一次模型调用、聊天回复

**分析运行**：
一次分析任务从开始到结束的可恢复执行记录，包含当前状态、分析计划、步骤结果、查询工件和失败信息；同一分析任务可以因重试或修订产生多个运行。
_Avoid_: HTTP 请求、聊天消息、模型调用

**分析计划**：
为完成一个分析任务而形成并向用户展示的有序分析步骤及其依赖关系；简单任务可以只有一个步骤，复杂任务可以逐步展开并修订。执行时逐项显示等待、进行中、完成或失败状态。
_Avoid_: SQL 执行计划、固定 Prompt 清单

**分析步骤**：
分析计划中可独立判定完成或失败的最小业务分析目标，必须说明步骤完成条件以及它所依赖的前序步骤。
_Avoid_: 模型思维过程、任意一次工具调用

**步骤完成条件**：
执行分析步骤前定义的数据需求与验收条件，用于判断实际查询结果是否足以完成该步骤，不能预设分析结论。
_Avoid_: 预期结论

**计划修订**：
分析运行根据新取得的查询结果，对尚未开始的分析步骤所做的可解释调整；已完成步骤不被改写。
_Avoid_: 静默改计划、重写历史、无限重新规划

**业务语义模型**：
对业务对象、关系、指标、维度和治理规则的版本化定义，是 DataAgent 理解和查询业务数据的唯一事实源。
_Avoid_: 散落的 Prompt 规则、硬编码 Schema 副本、聊天记忆

**渐进披露 Schema**：
系统不把全部表结构注入提示词。提示词只含紧凑目录（表名 + 一句话描述 + 关系），LLM 先基于目录选择相关表，再通过 `get_schema` 按需取该表的完整列定义。
_Avoid_: 全量 Schema 注入、把 DDL 全部塞进系统提示词

**紧凑目录**：
从业务语义模型按需派生的 Schema 摘要——每条目只有业务对象、一句话描述和关系，不含列级细节。是“渐进披露 Schema”的目录部分。
_Avoid_: SCHEMA_CATALOG 全量

**校验门**：
LLM 执行 SQL 前必须先通过 `plan_query` 获得计划（规范化 SQL、EXPLAIN QUERY PLAN、预估行数），再决定是否执行。校验与执行分离，失败带 `phase`（plan/validate/execute）。
_Avoid_: 校验与执行原子完成、直接 run_sql

**确认后记忆**：
用户显式确认（或纠正）一条问答后，问题与 SQL 才沉淀为可复用的 few-shot 记忆，以 Git 跟踪的 Markdown 为真相源。执行成功不等于可入记忆。
_Avoid_: 自动记忆、执行成功即入池

**查询工件**：
每次回答附带的结构化记录：查询计划、规范化 SQL、执行耗时、行数与图表。用于审计与复现。
_Avoid_: 只返回最终回答文本

**表上下文**：
单次请求中 LLM 实际引用的一组业务表。系统按它裁剪 Schema 检索与记忆检索，避免无关表干扰。
_Avoid_: 全库上下文、所有表都进提示词

**只读执行**：
Agent 对业务库只有只读权限，SQL 经 AST 白名单校验（表级、无写入操作）后执行。这是安全边界，不做敏感列拦截（本地部署无隐私顾虑）。
_Avoid_: 开放写入、列级隐私拦截
